<% flash.each do |flash_type, msg| %>
  <div class="alert alert-<%= flash_type %>" role="alert">
    <a href="#" class="close" data-dismiss="alert">×</a> <%#バツとじを利用すると違うページに遷移するエラーあり %>
    <%= msg %>
  </div>
<% end %>