<div class="list">
  <table class="table event_table">
    <thead class="thead-light">
      <tr>
        <th scope="col"><%= sort_link(q, :id, "No.") %></th>
        <th scope="col"><%= sort_link(q, :incident, "事象") %></th>
        <th scope="col"><%= sort_link(q, :status_id, "状態") %></th>
        <th scope="col"><%= sort_link(q, :coding_lang_id, "言語") %> </th>
        <th scope="col"><%= sort_link(q, :os_name_id, "OS") %> </th>
        <th scope="col"><%= sort_link(q, :created_at, "作成日時") %> </th>
      </tr>
    </thead>
    <tbody>
      <% incidents.each do |incident| %>
        <tr>
          <th data-label="No." scope="row"><%= link_to sprintf("%008d", incident.id), incident %></th>
          <td data-label="事象" class="w-incident"><%= incident.incident %></td>
          <td data-label="ステータス" class="w-status"><%= truncate(incident.status.status,length: 20) %></td>
          <td data-label="言語" class="w-coding-lang"><%= incident.coding_lang.name %></td>
          <td data-label="OS" class="w-OS"><%= incident.os_name.name %></td>
          <td data-label="作成日時" class="w-created-at"><%= l incident.created_at, format: :long %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>



